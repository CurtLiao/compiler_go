/* Sigma.go
 *
 * Compute sum = 1 + 2 + ... + n
 */

// variables
const n = 10
var sum int = 0
var index int = 0
  
// main function
func void main ( ) {
  

  // for (index = 0; index <= n + 6) {
  //   sum = sum + index
  //   index = index + 1
  // } 
  sum = 0 + 5 * 9 + -1 * 3
  // for (index <= n; index = index + 1 ) {
  //   // sum = sum + index  + 1 * 2
  //   sum = sum + index

  // } 
  // sum = 0
  // for (index = 0; index <= n + 6; index = index + 1) {
  //   sum = sum + index
    
  // } 
  print "The sum is "
  println sum
}


